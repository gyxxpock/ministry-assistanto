import { PLANNING_REPOSITORY_TOKEN } from './planning.tokens';

describe('PLANNING_REPOSITORY_TOKEN', () => {
  it('should be defined', () => {
    expect(PLANNING_REPOSITORY_TOKEN).toBeTruthy();
  });
});

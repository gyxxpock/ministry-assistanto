---
doc_type: drift-report
generated_by: documentation-reviewer
source_commit: 4b791f7bbe56a56c1106cb87e09fdf91820fd1e7
---

## 1. Stale architecture docs

- `docs/generated/architecture/dependency-overview.md`, `docs/generated/architecture/known-violations.md`: `source_commit` = HEAD (`4b791f7`), `git diff --name-only 4b791f7 HEAD -- src/app` is empty (trivially, it's HEAD), and `git diff --name-only 7203d53 HEAD -- src/app` (the graph's `built_at_commit`) is also empty — the graph is genuinely current relative to `src/app` (commit `4b791f7` only touched `graphify-out/` artifacts). `dependency-overview.md`'s God Nodes section was actually refreshed this pass (now correctly cites "graph built at commit `7203d53`, refreshed against HEAD at `4b791f7`"). Not stale.
- `docs/generated/modules/planning/overview.md`, `docs/generated/ai/planning/context.md`: `source_commit: e461526`. `git diff --name-only e461526 HEAD -- src/app` is non-empty and touches `facade/`/`presentation/` in `goals`, `planning`, and `time-entry` (e.g. `planning.facade.ts`, `planning.module.ts`, `goals.facade.ts`, `goals.module.ts`, `time-entry.tokens.ts`) — by the letter of this check, flag stale. However (see check 6) the actual diff on the two planning-specific files that changed is a single-line, same-length import-path swap that doesn't overlap either doc's cited `file:line` invariants, so this is closer to a front-matter/GRAPH_REPORT-citation staleness than a content-accuracy problem. Both docs also still cite `GRAPH_REPORT.md` as "built at commit `04892f9378025e48d69e69f896ce3151cea05cd5`" — the graph has been rebuilt twice since (now at `7203d53`, confirmed via `graphify-out/GRAPH_REPORT.md` line 13's "Built from commit: `7203d536`"). Current top-10 God Nodes (verified against the live `GRAPH_REPORT.md`) have shifted from what's cited: `WeeklySchedule` is now 22 edges/rank 8 (cited as 23/rank 9), `WeeklyScheduleEditorComponent` is now rank 9 (cited as rank 10); `TimeEntryFacade` (29, rank 3), `TimeEntry` (27, rank 4), `DexieTimeEntryRepository` (25, rank 5), `ITimeEntryRepository` (23, rank 6), `CourseVisit` (23, rank 7), and `GoalConfig` (21, rank 10) are current top-10 God Nodes not reflected in either doc. This is unchanged from the prior drift pass — still unresolved, recommend `/docs feature planning` or `/docs architecture`.
- `docs/curated/architecture/overview.md` and the ADRs: no `source_commit`, by schema design (`docs/_meta/schema/doc-manifest.schema.json:37-40` — the field is only present on `docs/generated/**` files). Not applicable.
- New this pass: `docs/_meta/doc-manifest.json` only carries 5 entries (`ai/planning/context.md`, `modules/planning/overview.md`, `architecture/dependency-overview.md`, `architecture/known-violations.md`, `drift/latest.md`). It is missing entries for at least 9 docs that exist on disk with valid front-matter: `docs/index.md`, `docs/curated/onboarding/getting-started.md`, `docs/curated/onboarding/module-tour.md`, `docs/curated/architecture/overview.md`, both ADRs, `docs/curated/workflows/feature-lifecycle.md`, and — new this session — all three `docs/curated/modules/{goals,planning,time-entry}.md` narrative pages. `documentation-agent.md`'s own "Responsibilities" say it updates the manifest after any specialist writes a doc; this hasn't happened for these files. Not itself a staleness bug (no per-file front-matter is wrong), but it degrades the manifest's usefulness as the drift-check source of truth going forward — flag as a bookkeeping gap for DocumentationAgent.

## 2. Outdated agent definitions

- `.claude/agents/ui-agent.md`: scope tree still cites `src/app/time-entry/presentation/models/time-entry.vm.ts` and `src/app/time-entry/presentation/tokens/time-entry.tokens.ts` — confirmed both missing (`test -e` fails on both). Current locations: `src/app/time-entry/facade/time-entry.vm.ts` and `src/app/time-entry/time-entry.tokens.ts` (feature root), moved in commit `7203d53`. `graphify explain "time-entry.tokens"` only resolves to the two real current nodes (`src/app/time-entry/time-entry.tokens.ts` and its `.spec.ts`) — the old `presentation/tokens/` path is confirmed dead in the graph too. Unresolved from the prior drift pass.
- `.claude/agents/facade-agent.md`: "Absolute restrictions" section still says the DI token is "defined in `tokens/time-entry.tokens.ts`" — stale path — and the scope tree omits `time-entry.vm.ts`, which now correctly lives in this layer per `known-violations.md`'s own resolution text. This agent file's last-modified commit (`88822c5`) predates the `7203d53` move. Unresolved from the prior drift pass.
- `.claude/agents/domain-agent.md`: scope tree still lists `utils/file-util.service.ts` under `src/app/time-entry/domain/` with a "⚠️ review whether it belongs here" comment, but `known-violations.md`'s "Resolved since last review" section confirms `FileUtilService` now lives at `src/app/core/services/file-util.service.ts`. Last touched in `7203d53` — the same commit fixed this doc's other stale reference but left this one half-fixed. Unresolved from the prior drift pass.
- `.claude/agents/compodoc-agent.md`, `.claude/agents/documentation-agent.md`, `.claude/agents/human-docs-agent.md` (all modified this session, uncommitted): scope trees were checked directly against the working tree — `docs/generated/api/**` (populated, real Compodoc output), `.compodocrc.json`, `mkdocs.yml`, `docs/_meta/**`, `docs/curated/modules/*.md` all exist and match what each file describes. No drift found. (Their `git log -1` timestamps predate this session's edits only because nothing has been committed yet — content was verified by direct read, not by commit-date comparison.)
- Cross-cutting gap (informational, unchanged from prior pass): `domain-agent.md`, `data-agent.md`, `facade-agent.md`, `ui-agent.md`, `testing-agent.md`, and `architecture-guardian.md` all still hardcode `time-entry`-only paths/symbols, while `AGENTS.md`'s role table implies generic coverage of `goals`/`planning` too. Longstanding mismatch, not new drift — flagging because it's agent-file content that only a human or the named specialist should edit.

## 3. Moved or renamed files

- The only dead paths found are the two already identified in check 2, both inside agent definitions: `src/app/time-entry/presentation/models/time-entry.vm.ts` and `src/app/time-entry/presentation/tokens/time-entry.tokens.ts` (cited in `ui-agent.md`/`facade-agent.md`). Resolved current locations: `src/app/time-entry/facade/time-entry.vm.ts` and `src/app/time-entry/time-entry.tokens.ts`. Flagging directly rather than auto-fixing, since it touches `.claude/agents/*.md` content.
- `feature/Plan.md` and `feature/Goals.md` (cited from `docs/generated/modules/planning/overview.md` and the new `docs/curated/modules/goals.md`/`planning.md`): both confirmed to exist at repo root.
- Verified every relative markdown-link target and every inline-code path citation across all of `docs/**` (67 links, 64 unique path-like inline-code tokens) — no other dead paths found, aside from the deliberate ADR template placeholder `NNNN-slug.md` (see check 4) and historical "previously lived at X" citations in `known-violations.md`'s "Resolved since last review" section, which correctly describe now-deleted old paths as history, not as live citations.

## 4. Broken doc-to-doc links

- (none found) — all 67 relative markdown links (including anchors) across `docs/**/*.md` resolve, including in the files new/changed this session: the three `docs/curated/modules/*.md` pages, the updated `docs/curated/onboarding/module-tour.md`, and `docs/index.md`. This includes cross-file anchors like `docs/generated/modules/planning/overview.md` → `ai/planning/context.md#god-nodes--edges-in-scope` and the new curated pages' self-referencing `#where-to-go-next` anchors — both verified against actual heading slugs.
- The only non-resolving pattern is the intentional ADR template placeholder `[ADR-NNNN](NNNN-slug.md)` at `docs/curated/architecture/adr/0000-template.md:17` (and referenced again at `:40`) — a known, deliberate placeholder, not real breakage; also flagged as such in the prior drift report.
- `getting-started.md`, `module-tour.md`, and `feature-lifecycle.md` do link out to `../../../AGENTS.md` and `../../../CLAUDE.md#documentation`/`#orchestration` (the new `curated/modules/*.md` pages themselves do not). These resolve fine as plain repo-relative links and the anchors exist (`AGENTS.md` has `## Orchestration`, `CLAUDE.md` has `## Documentation`). This is an existing, deliberate pattern (linking out of the docs tree to repo-root docs), not new breakage — not flagged as drift. Noting only as a pre-existing MkDocs-build-only caveat (out-of-`docs_dir` targets produce MkDocs "target not found" warnings, not actual dead links).

## 5. Feature/module mismatch

- `planning`: fully covered — `docs/generated/modules/planning/overview.md` and `docs/generated/ai/planning/context.md` both exist with matching `module: planning` front matter.
- `goals`: missing generated docs — has all 4 layer subfolders but no `docs/generated/modules/goals/overview.md` or `docs/generated/ai/goals/context.md`. Expected/known gap: `docs/curated/modules/goals.md` (new this session, narrative-only) explicitly flags this itself in its own "Where to go next" section.
- `time-entry`: missing generated docs — has all 4 layer subfolders but no `docs/generated/modules/time-entry/overview.md` or `docs/generated/ai/time-entry/context.md`. Same expected gap; `docs/curated/modules/time-entry.md` (new this session) self-flags it too.
- `shared`: only has `domain/`+`presentation/` (no `data/`/`facade/`), so doesn't meet this check's 4-layer criterion — not flagged as a coverage gap.
- `docs/generated/ai/index.json` is still `{"docs": []}` even though `docs/generated/ai/planning/context.md` exists — the retrieval index remains stale/never populated. Unresolved from the prior pass.
- Related bookkeeping note (see check 1): `docs/_meta/doc-manifest.json` doesn't yet track the two new missing-doc-but-narrative-exists cases (`goals`, `time-entry`) or any curated doc besides `modules/planning/overview.md`/`ai/planning/context.md` — worth fixing alongside the generated-doc gaps above.

## 6. Stale invariants

- `docs/generated/ai/planning/context.md`: re-checked every cited `file:line` invariant against the diff since `e461526` — `planning.usecase.ts:17-18,34-44,70-74` and `dexie-planning.repository.ts:21-25` are in files with zero changes in that range; `planning.facade.ts:50-52,74` and `planning.module.ts:35-40` are in files that did change, but in both cases the diff is a single-line, same-line-count import-path swap (`../../time-entry/presentation/tokens/time-entry.tokens` → `../../time-entry/time-entry.tokens`) at an unrelated line (line 5 and line 26 respectively) that doesn't overlap the cited ranges. Spot-verified the new import target and every other cross-referenced citation in `known-violations.md` (`time-entry.tokens.ts:4`, `time-entry.facade.ts:6`, `goals.facade.ts:7`, `planning.module.ts:68-71`, `time-entry.module.ts:60,65`, `app-routing-module.ts:11-12`) directly against current file contents — all accurate. None of the literally-cited invariants are stale by the letter of the check.
- Same doc's un-lined "Dependencies" narrative still states `TIME_ENTRY_REPOSITORY` is "a presentation-layer token consumed by planning's facade" — factually wrong now; the token lives at `src/app/time-entry/time-entry.tokens.ts` (feature root, not Presentation), exactly the fix `known-violations.md` documents as resolved. Unresolved from the prior pass — recommend `ai-docs-agent` rewrite this section on the next `/docs feature planning` run.
- `docs/generated/ai/invariants.md` and `docs/generated/ai/_template-context.md`: both carry `source_commit: <pending-first-generation>` placeholders — out of scope for this check.
- No `docs/generated/ai/goals/context.md` or `docs/generated/ai/time-entry/context.md` exist yet (see check 5), so there is nothing further to check for those modules.

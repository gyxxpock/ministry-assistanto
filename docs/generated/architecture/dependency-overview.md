---
doc_type: architecture-generated
module: root
source_commit: e461526035a179d92b034f04ec05b2f3ed670980
generated_by: architecture-doc-agent
status: draft
sources:
  - graphify-out/graph.json
  - graphify-out/GRAPH_REPORT.md
  - .claude/agents/architecture-guardian.md
---

# Dependency overview (generated)

> Regenerated by `architecture-doc-agent` from `graphify-out/` and
> `architecture-guardian`'s live review. Never hand-edit — run `/docs architecture` to
> refresh. The God Nodes table below is grounded in the `planning` module's real
> graphify data (first worked example); other modules' God Nodes still need a full
> `/docs architecture` regeneration pass to be added here.

## Layer dependency diagram

```mermaid
flowchart LR
    Presentation --> Facade
    Facade --> Data
    Facade --> Domain
    Data --> Domain

    classDef domain fill:#dff0d8,stroke:#3c763d;
    classDef data fill:#fcf8e3,stroke:#8a6d3b;
    classDef facade fill:#d9edf7,stroke:#31708e;
    classDef presentation fill:#f2dede,stroke:#a94442;

    class Domain domain;
    class Data data;
    class Facade facade;
    class Presentation presentation;
```

Allowed direction is inward only (Presentation → Facade → Data/Domain, Data → Domain).
**No layer may depend on a more outer one** — Domain must never import Data or
Presentation. Full rule text lives once in
[CLAUDE.md § Arquitecture Rules](../../../CLAUDE.md#arquitecture-rules-clean-architecture);
this diagram illustrates it, it doesn't redefine it.

## God Nodes

Per `graphify-out/GRAPH_REPORT.md` (built at commit
`04892f9378025e48d69e69f896ce3151cea05cd5`), ranks 9 and 10 of the repo's
top-10 God Nodes live in the `planning` module:

| Node | Edges | Rank | Layer / owning agent | Module doc |
|------|-------|------|-----------------------|------------|
| `WeeklySchedule` | 23 | 9 | Domain — `domain-agent` | [planning/overview.md](../modules/planning/overview.md#god-nodes-in-this-module) |
| `WeeklyScheduleEditorComponent` | 21 | 10 | Presentation — `ui-agent` | [planning/overview.md](../modules/planning/overview.md#god-nodes-in-this-module) |

`TimeEntryFacade` and `TimeEntry` (ranks unconfirmed against the current graph
build) are also flagged as God Nodes by `architecture-guardian`
(`.claude/agents/architecture-guardian.md:133`) but have not yet been
cross-referenced here — pending a `/docs architecture` pass over the
`time-entry` module.

## How this doc is produced

```bash
graphify update .
graphify query "layer dependency violations"
/docs architecture   # architecture-doc-agent regenerates this file
```
